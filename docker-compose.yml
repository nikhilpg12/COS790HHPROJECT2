services:
  gp:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      OLLAMA_HOST: http://host.docker.internal:11434
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./data:/app/data:ro
      - ./results:/app/results
    working_dir: /app
